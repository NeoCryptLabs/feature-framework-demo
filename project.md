Create a SaaS analytics dashboard called "PulseBoard" to track online business performance.

Features: authentication with roles (admin/viewer), dashboard with stat cards and charts, analytics page, settings (admin only). All data from a real database with seed data.
